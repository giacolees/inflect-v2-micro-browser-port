User explicitly approved GPL-3.0-or-later distribution for this plugin, so use eSpeak-NG WASM rather than a substitute G2P. Select/pin an eSpeak-NG WASM package with en-US data, carry its GPL and third-party notices plus source/build provenance, then port the upstream normalization/token-ID/chunking behavior into the browser. Validate normalized text, phoneme string, IDs, and 512-token chunk handling against the Python VITS frontend corpus. The browser synthesis path must remain fully local and must not invoke Python/native ORT/remote inference at synthesis time.