Implement a browser-safe copy of the Inflect text frontend without changing the TTS model or using Python at browser synthesis time. Preserve upstream normalization semantics, use a local eSpeak-NG WASM implementation/data under compatible notices, and generate the exact symbols/blank-interspersed IDs expected by the 512-token core. Validate normalized text, phoneme string, tokens, and IDs against the committed Python fixtures for the six prompt categories. Define chunking behavior compatible with upstream split_text and reject any chunk above 512 IDs. Keep model/runtime blobs ignored. This plan may end NO-GO if no lawful/maintained browser eSpeak implementation can reproduce the configured frontend.